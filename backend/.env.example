# Supabase project URL (Settings → API → Project URL)
SUPABASE_URL=https://your-project.supabase.co

# Supabase API key (Settings → API → service_role for full storage access)
SUPABASE_KEY=eyJ...your-key

# Direct Postgres connection (Settings → Database → Connection string → URI)
SUPABASE_DB_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# Google Gemini API key for AI-powered space parsing
GEMINI_API_KEY=AIza...your-key

# Path to pptx-engine (relative to backend/ or absolute)
PPTX_ENGINE_PATH=../pptx-engine

# Supabase Storage bucket for generated PPTX files
STORAGE_BUCKET=presentations

# Frontend URL (used for CORS)
FRONTEND_URL=http://localhost:3000
